# Q-STPP v15 (corrected) — actual runtime dependencies.
# The pipeline is pure classical NumPy/SciPy; there is no quantum backend,
# no PyTorch, no PennyLane/Qiskit, no SDP solver.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
